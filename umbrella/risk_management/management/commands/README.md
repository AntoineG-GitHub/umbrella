# Management Commands for Risk Management

This folder contains Django custom management commands to compute risk metrics over the portfolio.

## Commands

```bash
python manage.py compute_var --date=YYYY-MM-DD
```